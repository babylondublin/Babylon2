extends ../../../layouts/default

include ../../../mixins/classified

block intro
	.page-intro: .container
		h1.page-title Clasificados
		hr
		if user
			a(href='/me/create/classified') crear clasificado
		if !user
			a(href='/join') Regístrate para crear clasificado
		hr

block content
	.page-content: .container.blog
		.row
			div(class= !data.classifieds.length ? 'col-md-10' : 'col-md-10 col-md-offset-1')
				if data.classifieds.length
					.post-cards.row(itemscope itemtype="https://schema.org/Blog")
						each classified in data.classifieds
							+classified(classified)
				else
					h3.text-muted No hay clasificados aún.